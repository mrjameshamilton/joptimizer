<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" monitorInterval="60">
    <Properties>
        <Property name="filePattern">%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n</Property>
        <Property name="startupTs">${date:yyyy-MM-dd_HH-mm-ss}</Property>
    </Properties>

    <Appenders>
        <!-- Console: INFO only, no level marker -->
        <Console name="ConsoleInfo" target="SYSTEM_OUT">
            <PatternLayout pattern="%msg%n"/>
            <Filters>
                <LevelRangeFilter minLevel="INFO" maxLevel="INFO" onMatch="ACCEPT" onMismatch="DENY"/>
            </Filters>
        </Console>

        <!-- Console: WARN..FATAL with level marker -->
        <Console name="ConsoleWarnPlus" target="SYSTEM_OUT">
            <PatternLayout pattern="[%level] %msg%n"/>
            <Filters>
                <!-- Only WARN and above -->
                <ThresholdFilter level="WARN" onMatch="ACCEPT" onMismatch="DENY"/>
            </Filters>
        </Console>

        <!-- File: all levels with details -->
        <RollingFile
            name="File"
            fileName="logs/app-${startupTs}.log"
            filePattern="logs/app-%d{yyyy-MM-dd}-%i.log.gz">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n"/>
            <Policies>
                <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
                <SizeBasedTriggeringPolicy size="10 MB"/>
            </Policies>
            <DefaultRolloverStrategy max="7"/>
        </RollingFile>
    </Appenders>

    <Loggers>
        <!-- TRACE so file gets everything; console split by filters -->
        <Root level="TRACE">
            <AppenderRef ref="ConsoleInfo"/>
            <AppenderRef ref="ConsoleWarnPlus"/>
            <AppenderRef ref="File"/>
        </Root>
    </Loggers>
</Configuration>